/* function square(x) {
  return x * x;
}

function doubler(amount) {
  return 2 * amount;
}

function interest(principal, rate, period) {
  return principal * rate * 0.01 * period;
}
 */

const square = (x) => x * x;
const doubler = (amount) => 2 * amount;
const interest = (principal, rate, period) => principal * rate * 0.01 * period;
